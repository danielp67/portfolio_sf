/* On css
#project{
    overflow: hidden;
    transition: transform 1000ms;
}
 */

let y = 0
let article = document.querySelector("#project");
/*
article.addEventListener('wheel', (e) => {
    const height = article.offsetHeight

    y = y + e.wheelDeltaY

    if(y < -height + window.innerHeight) {
        y = -height + window.innerHeight
    }

    if(y > 0) {
        y = 0
    }

    const tr = `translateY(${y/3}px)`

    article.style.transform = tr

})
*/
